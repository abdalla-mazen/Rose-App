declare type userAddress = {
  street: string;
  phone: string;
  city: string;
  lat: string;
  long: string;
  username: string;
  _id?: string;
};

declare type Coordinates = { lat: number; lng: number };
